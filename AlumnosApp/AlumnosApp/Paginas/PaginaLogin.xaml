<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AlumnosApp.Paginas.PaginaLogin">
    <StackLayout VerticalOptions="Center">
        <Label Text="Usuario: " Style="{StaticResource LabelTitulo}" Margin="10,0,10,5"/>
        <Entry x:Name="txtUsuario" Style="{StaticResource CajaTexto}" Margin="10,5,10,10"/>

        <Label Text="Password: " Style="{StaticResource LabelTitulo}" Margin="10,10,10,5"/>
        <Entry x:Name="txtPassword" IsPassword="True" Style="{StaticResource CajaTexto}" Margin="10,5,10,10"/>

        <ActivityIndicator x:Name="activityIndicator" Style="{StaticResource Indicador}"/>
    </StackLayout>

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="Login" Text="Iniciar sesión" Order="Primary" Priority="0" Clicked="Login_Clicked"/>
    </ContentPage.ToolbarItems>
</ContentPage>